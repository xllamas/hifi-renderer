org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# NOTE: this file must stay machine-independent -- it is committed.
# Gradle needs JDK 17+. If your system `java` is older, set
# org.gradle.java.home in ~/.gradle/gradle.properties (not here), or export
# JAVA_HOME. See README.md.
